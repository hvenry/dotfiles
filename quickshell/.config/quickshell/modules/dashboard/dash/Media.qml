import QtQuick

// Empty media component - all media controls moved to dedicated Media tab
Item {
    id: root

    anchors.top: parent.top
    anchors.bottom: parent.bottom
    implicitWidth: 0
    visible: false
}
